{
  "name": "wit-dashboard",
  "version": "1.3.5",
  "private": true,
  "description": "Buildless, local-first personal student dashboard",
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "check": "node --check dashboard-ui.js && node --check app.js && node --check sw.js"
  },
  "devDependencies": {"jsdom": "30.0.1"},
  "engines": {"node": ">=22.13.0"}
}
